# Changelog Archive

This archive is for versions 1.5.1 and prior. For current changes see [CHANGELOG.md](./CHANGELOG.md).

---

## [1.5.1]

Say goodbye to "pins" and hello to "shortcuts" alongside a massive, beautifully visual upgrade to how you schedule and plan your workflows. [log](https://github.com/saropa/saropa_workspace/blob/v1.5.1/CHANGELOG.md)

### Added

- **Set a shortcut's schedule in one form instead of a one-field-at-a-time menu.** **Configure Schedule...** now opens a single screen — daily time, days of the week, repeat interval, an advanced cron field with one-click presets, run-on-open, and enabled — all visible at once with inline descriptions and a live **Next run** preview that updates as you change fields. Setting a time turns the schedule on automatically, and the time and interval you used are remembered as the starting point for the next shortcut you schedule. The previous keyboard-only menu stays available as **Configure Schedule (Quick)...**.
- **See how a shortcut's schedule fits your day.** The schedule form has an **Around your schedule** section: a 24-hour timeline that plots this shortcut's daily time against every other scheduled shortcut, so clustering and quiet stretches are visible at a glance. It warns when another shortcut runs in the **same minute**, naming which ones, and calls out the **largest free stretch** of the day. The marker slides live as you change the time.

### Changed

- **Pins are now called Shortcuts.** Everything that was a "pin" is now a **shortcut**: the view is **Shortcuts**, the groups are **Project Shortcuts** and **Global Shortcuts**, a collection is a **Shortcut Set**, and the commands read **Add Active File as Shortcut**, **Remove**, **Run Shortcut...**, **Promote to Shortcut**, and so on. Only the wording changed — your existing shortcuts, shortcut sets, keybindings, and shared links all keep working exactly as before. (VS Code's own "pinned tab" feature still uses its own name; the suggestion to add a long-pinned tab now offers to add it to your Saropa **shortcuts**.)
- **The Recent list now includes files you open, not just shortcuts you run.** A saved file you open — by clicking its shortcut, or by any other means (Ctrl+P, the Explorer, switching tabs) — or close now appears in the **Recent** group in the Shortcuts view, tagged *(opened)*. Opening a file is not counted as a run, so it never changes a shortcut's run count or the most-run analytics. Focusing or closing a saved file also clears it from the activity-bar badge that counts unopen shortcuts.
- **The planner screen now carries the Saropa name.** The Schedule & Workflow Planner panel's tab and heading now read **Saropa Schedule & Workflow Planner**, matching the Saropa Dashboard, so every full-screen surface is branded consistently.
- **Click a scheduled item in the planner to see its details.** Clicking a block in the Week view (or a marker in the Day view) now highlights it and opens its details — schedule, triggers, and the Run / Open / Schedule / Triggers actions — in the right-side inspector panel. The selected item is outlined so you can tell which one the inspector describes.
- **Choose compact or comfortable row height in the planner.** A new **Compact / Comfortable** toggle in the planner toolbar switches the Day and Week time grids between the dense overview and a roomier layout with double the per-hour height. Your choice is remembered for the panel.
- **The planner's Workflow tab no longer scrolls forever.** Shortcuts that aren't part of a chain or event link now live in a compact, collapsible **Unlinked shortcuts** shelf below the canvas, so the canvas shows only the shortcuts that are actually wired together. Drag a shortcut from the shelf onto a step to run it after that step. Includes an **Auto-arrange** button to lay the chains into tidy columns, an **Add link** button for a searchable shortcut-to-shortcut link builder, and a text filter box for shelves with more than a dozen unlinked shortcuts.
- **A selected item's details now open in their own panel on the right.** Clicking an item in the planner opens its details in a dockable inspector column on the right side — like the Workflow toolbox — instead of a strip below the grid that scrolled out of sight. The panel stays in view as you scroll the Day/Week grid.
- **The planner's side columns are now resizable.** Drag the edge of the right-side detail inspector, or the right edge of the Workflow toolbox, to set its width — each remembers its size for the panel.
- **The planner inspector now explains what a recipe does.** Selecting a scheduled recipe — like **Workspace bloat scan** — shows an info note describing what it does and what it was detected from, so a seeded or paused item explains itself in place.
- **Pause or resume a schedule straight from the planner inspector.** When the inspector shows a scheduled shortcut as *(paused)*, it has a **Resume** button (and a **Pause** button when active) beside Run / Open / Schedule / Triggers. The action names the shortcut in a confirmation toast.
- **The Shortcuts title `···` menu is now flyout submenus instead of one long list.** The overflow menu that used to drop a single scroll of ~30 items now opens six cohesive flyouts — **Add to Shortcuts**, **Editor Layout & Focus**, **Import & Suggest**, **Shortcut Sets**, **Run & Diagnostics**, and **Workspace & Schedule** — so each action is one hover away in a labeled group.
- **Search for shortcuts by icon synonym.** Each icon now carries a keyword list (shown beside its name and matched on as you type), so an alternate word finds it even when you don't know the exact name — "settings" or "cog" surfaces the gear, "octocat" surfaces GitHub, "deploy" or "launch" surfaces the rocket.

### Fixed

- **The Schedule, Triggers, and Boot Sequence menus no longer jump back to the top after every change.** Each menu now keeps your place on the row you just changed and stays open when focus shifts, so building up a schedule is a steady sequence ending at **Save changes** rather than a list that resets under you.
- **Setting a time on a schedule now turns it on automatically.** Giving a shortcut a daily time, an interval, a cron, or run-on-workspace-open now flips **Enabled** on for you. Switching **Enabled** off yourself still sticks: a deliberate "keep this schedule but pause it" is respected.
- **The Shortcuts toolbar's Run Shortcut button no longer looks like "run all".** The **Run Shortcut...** action in the Shortcuts view title bar now uses the single play triangle instead of the double-arrow icon, matching the per-shortcut inline run button since it runs one shortcut you pick.

---

## [1.5.0]

Running shortcuts gets smarter and friendlier: drop a file straight onto a script to run it on that file, peek and simulate a run before you commit, diff a task's last two runs, auto-copy a value out of noisy output, and one-click the fix a failed run suggested. Plus a throwaway scratchpad, saved editor layouts, `.env` profile switching, shell-history shortcut suggestions, optional audio cues, and a reusable Preview tab — alongside a grouped, searchable icon picker, a cleaner administrator flow, and pop-out window fixes. [log](https://github.com/saropa/saropa_workspace/blob/v1.5.0/CHANGELOG.md)

### Added

- **Run a shortcut automatically when another file changes.** Right-click a script shortcut and choose **Run This Shortcut When a File Changes** to link it to one or more files or glob patterns — pick a specific file, or type a pattern like `**/*.graphql` or `src/**`. From then on, saving any matching file runs that shortcut in the background, so editing `schema.graphql` can regenerate your types without you remembering to run the script. This is the cross-file companion to the existing **Run on save** toggle (which runs a shortcut when its *own* file is saved): here the shortcut and the watched file are different. A rapid save burst runs the shortcut at most once per short cooldown, and the run is forced to the background so it never steals your terminal; the output channel logs which file triggered it.
- **Mask a sensitive shortcut so it can't flash on a shared screen.** Right-click a file shortcut and choose **Mask / Reveal Shortcut (Screen-Share Guard)** to hide it: in the Shortcuts view it shows a generic **Protected file** label and a lock icon instead of its name, and its real path is kept out of the row and the hover tooltip — so a saved `.env.production` reveals nothing while you're presenting or on a call. Opening a masked shortcut first asks **Reveal `.env.production`?** in a confirm dialog, so a stray click can't instantly display the file; choosing the same menu item again reveals the shortcut permanently. This guards the open and hides the name — it does not blur the contents of a file you've already opened (no editor API can redact open text), so for true secrecy keep the file encrypted or removed.
- **Let your shortcut set follow the git branch you're on.** Turn on `saropaWorkspace.branchAware.enabled`, then choose **Link Current Branch to Shortcut Set...** to bind the branch you're on to a shortcut set. From then on, checking out that branch switches the Shortcuts view to its set automatically — your release branch shows your release shortcuts, a feature branch shows that feature's working files — with a toast naming the set and branch. You can also designate one shortcut to run on the switch (e.g. refresh dependencies), which runs through the normal runner so its output is visible. **Unlink Current Branch from Shortcut Set** removes the binding. Off by default, and inert outside a git repository, so single-set and non-git workspaces are unchanged. Bindings are kept per-workspace on your machine.
- **Drop a file onto a script shortcut to run it on that file.** Drag a file from the Explorer and drop it onto a runnable shortcut (say `process_image.py`) to run the script against that file. The dropped path is available as a new `$droppedFile` token you can place anywhere in the shortcut's command or arguments; if the shortcut doesn't use the token, the path is appended as the final argument, so a plain script just receives the file. The shortcut's saved configuration is unchanged — the file applies to that one run.
- **One-click the fix your failed run suggested.** When a background run fails and its output names a fix command — `Run \`npm install lodash\` to fix`, a bare `pip install …`, `yarn add …` — the failure toast now offers a **Run: …** button that executes that exact command in the integrated terminal, so you don't have to select, copy, and paste it. The button always shows the full command, so you run it knowingly; if no recognized suggestion is found, the toast is unchanged.
- **Pull one value out of a noisy run into your clipboard.** In **Configure Run** there's a new **Extract from output** field: give it a regular expression (e.g. `Live at: (https://\S+)`) and when the shortcut finishes a background run, Saropa Workspace copies the first capture group — the deploy URL, a generated id — straight to your clipboard with a toast, instead of making you hunt for it in hundreds of log lines. Applies to background runs; an invalid pattern or no match is noted in the output channel and otherwise ignored.
- **Diff a shortcut's last two background runs.** Re-running a failing background task and not sure whether the error is the same one or a new one? Right-click the shortcut and choose **Diff Last Two Runs** to open a native side-by-side diff of the previous run's output against the latest, so the lines that changed stand out. Outputs are kept in memory for the current session only.
- **Share a shortcut as a one-click link.** Right-click a shortcut and choose **Copy as Saropa Link** to put a `vscode://` import link on your clipboard, carrying that shortcut's exact configuration (command, arguments, environment, macro steps, icon). Paste it in chat; when a teammate clicks it, VS Code asks to import — showing what the shortcut does first — and adds it. Importing never runs the shortcut, so a shared command is always a visible, deliberate choice.
- **Parameterized shortcuts remember your last answer.** A shortcut that asks for a value at run time (`${prompt:Environment}` or `${pick:dev,staging,prod}`) now defaults to whatever you chose last — the input box is pre-filled, and the picker highlights your previous choice — so re-running with the same value is a single Enter. A new **Run with Last Parameters** action on the shortcut's menu skips the questions entirely and runs with the remembered values (asking only for any parameter you have never answered). Remembered values are kept on your machine, per workspace, and are dropped when the shortcut is removed.
- **Peek a saved file without leaving your editor.** Right-click a file shortcut and choose **Peek** to float its contents in an inline overlay over your current editor, anchored at the cursor — no new tab, no focus stolen. Press Escape to dismiss it and keep typing. Useful for glancing at a constant or a type definition in another file while you stay in the one you're editing. With the Shortcuts view focused, **Alt+P** peeks the selected shortcut (rebindable in Keyboard Shortcuts).
- **Simulate Run — audit exactly what a shortcut will do before you run it.** Right-click any shortcut and choose **Simulate Run** to open a read-only preview of the exact command line, working directory, run location, and environment variables a real run would use — with `$workspaceRoot`/`$file` tokens already resolved and any `${prompt:…}`/`${pick:…}` questions answered virtually. Nothing is executed, so you can safely inspect a shared macro or a complex run config before double-clicking it. Recipes (open-a-URL, run-a-command, multi-step macros) show what each step would do.
- **Hear when a run starts and how it ended — audio cues.** Turn on `saropaWorkspace.sound.enabled` to play a short cue when a shortcut run starts, and a distinct success or failure tone when it finishes — so a long build or an unattended job announces its outcome without you watching the output channel. Off by default; the cue is additive to the on-screen toast, and it uses your operating system's own built-in sounds (so it follows the OS volume and mute, with no bundled audio and no extra permissions). Per-event toggles (`onStart` / `onSuccess` / `onFailure`) choose which moments chime, and each shortcut's **Configure Run** has an **Audio cues** field to force the cues on for one job or silence a chatty one. Background and report runs report a real outcome; terminal and external-window runs can't be tracked to an exit, so they cue only on start. (Haptics have no VS Code extension API and are deferred — audio ships first.)
- **Open a throwaway scratchpad that never dirties git.** A new **New Scratchpad** action (Shortcuts title menu `···`, or the command palette) opens a fresh in-memory buffer in the format you pick (Markdown, JSON, SQL, JavaScript, or plain text) — the clean place to format a snippet or test a query instead of creating `temp.json` / `scratch.md` in the repo root. It lives only while VS Code is open, never touches disk, and never shows in `git status` unless you deliberately save it somewhere.
- **Save and restore an editor layout in one click.** Arrange your editors the way a feature needs them — `Hero.tsx` left, `hero.module.css` right, `types.ts` in a third column — then **Save Editor Layout** (Shortcuts title menu `···`, or the command palette) names that grid. **Restore Editor Layout** recreates the columns and reopens every file in its place, so setting up a feature's working set is one pick instead of six drags. Layouts are remembered across workspaces; saving under an existing name updates it; a file that has since moved or been deleted is skipped and counted in the restore message, never blocking the rest.
- **Turn the commands you keep retyping into shortcuts — from your shell history.** A new **Suggest Shortcuts from Shell History** action (Shortcuts title menu `···`, or the command palette) scans your local shell history (PowerShell PSReadLine, bash, zsh), finds the complex one-liners you've typed at least three times — the `docker exec`, `psql`, `ssh`, `curl` invocations you retype — and offers them in a multi-select list with how often each appeared. Pick the ones you want and they're saved as global shell shortcuts, double-click to run. The scan is read-only and entirely on this machine — your history is never modified and never transmitted — and nothing is added or run until you choose it.
- **Switch your active `.env` between profiles in two clicks.** If your project keeps `.env.staging`, `.env.prod`, `.env.local`, the new **Switch .env Profile** action (Shortcuts title menu `···`, or the command palette) lists them — marking which one is active now — and copies the one you pick over `.env`. If your current `.env` has hand edits that match no profile, it asks first and backs the file up to `.env.bak` before overwriting, so manual changes are never lost. Switching between recognized profiles needs no backup (the previous environment still lives in its own file). After the swap it reminds you to restart your dev server to apply the new values.
- **Open shortcuts in a reusable Preview tab.** Turn on `saropaWorkspace.previewMode.enabled` and a single click on a saved file opens it in a transient Preview tab (italic title), just like the native Explorer — clicking another shortcut reuses the same tab instead of opening a new one, so clicking through a group of reference files (config variants, log files) no longer floods the editor with tabs. Editing the file promotes the tab to permanent automatically; double-clicking a non-runnable shortcut promotes it too. Off by default, so existing shortcuts keep opening as permanent tabs.
- **Importing favorites now keeps your groups.** When you import from the kdcro101 "Favorites" extension, each of its folders/groups becomes a matching shortcut group and its files are filed inside it, instead of the group being dropped — so your organization survives the move. Re-running the import never creates a duplicate group or shortcut (groups are reused by name), and a folder favorite that has no shortcut equivalent is reported in the output channel rather than silently skipped.
- **The "import your old favorites" offer now appears when a favorites file shows up later.** The one-time prompt to import from another favorites extension previously only checked at startup; it now also appears if a `.favorites.json`, an oleg-shilo list, a Bookmarks file, or a recognized favorites setting is added or changes while you're working — for instance after you install another favorites extension, or pull a teammate's file. It still asks at most once per workspace until you import or dismiss it.
- **Importing from the Favorites Panel extension now also reads its custom file.** If you keep your Favorites Panel items in a separate JSON file via its `favoritesPanel.configPath` or `favoritesPanel.configPathForWorkspace` setting, those items are now imported alongside the in-settings ones, with the same mapping (open-file, run, open-URL, command, and macro sequences). Both file layouts are understood — the newer top-level list and the older `{ "favoritesPanel.commands": [...] }` wrapper — and an item that already exists is never duplicated.

### Changed

- **The Saropa Suite shortcuts now group by tool.** When a project is wired to more than one sibling Saropa tool, the detected shortcuts under **Saropa Suite** are now filed into a subfolder per tool — **Saropa Lints**, **Drift Advisor**, **Log Capture** — instead of sitting in one long flat list. A subfolder appears only for a tool that's actually present, and the one-click **Boot the Saropa suite** macro stays at the top of the Saropa Suite group. A single-tool project looks the same as before.

- **The shortcut right-click menu is regrouped into clear sections.** A shortcut's context menu is now organized into labeled blocks separated by dividers — **run** actions (Open, Run, Run with last parameters, Stop, Peek, Simulate, Show Output, Diff), **configure** actions (Configure Run / Schedule / Triggers, Pause, Appearance, metric, tail, tag, branch link, expiry), **edit** (Rename, Promote, New Routine, Use as Template, Workspace Shortcut), **file** operations (New / Duplicate / Rename / Copy / Lock / Delete), **copy** (Copy Path, Copy as Saropa Link), and **annotate** (Add Comment / Separator) — so related actions sit together instead of in one long list.
- **Turning on administrator privileges is now a single flow.** Choosing the external window now immediately asks whether to run as administrator, instead of returning to the settings menu where the toggle only appears after the fact. The toggle still lives on the settings menu, so it remains adjustable later.
- **Choosing a shortcut's icon is now one grouped, searchable list with many more icons.** The icon picker replaces the old flat list with scannable categories — Files & code, Run & build, Source control & cloud, Data & terminal, Status & alerts, Shapes & color, and Objects & places — and you can type the icon name to filter instead of scrolling.

### Fixed

- **Running a script as administrator now opens the elevated window.** Previously, choosing the external window with **Administrator privileges** did nothing — no UAC prompt and no window — because the launcher was started in a mode that silently canceled the elevation request. The elevated window now opens (with the usual Windows UAC prompt).
- **The run-settings editor no longer discards your edits on a misclick.** The settings menu and every step within it now stay open when you click elsewhere, so an accidental click outside the picker no longer closes the editor and loses everything. Only pressing Escape cancels.

<details><summary>Maintenance</summary>

- Centralized the Shortcuts tree's row glyphs and tints in one token module (`views/pinRowTokens.ts`). The row builder previously decided every codicon and theme color inline in a long priority chain; that decision now lives behind `resolvePinRowIcon`, which owns the state priority while named branches own each glyph/tint, with a legend of what each reads as. Pure extraction — byte-for-byte the prior behavior, no visual change — so a future row state has one place to add its glyph. No user-facing change.
- Automated version numbering in the release script (`scripts/publish.py`). A full publish now resolves the version itself: it offers a default (a patch bump when the changelog has an `[Unreleased]` section, otherwise the current `package.json` value, and never below a version already written ahead by hand), prompts with an editable timeout, writes `package.json`, renames `## [Unreleased]` to the cut version, reconciles the two, and bumps past any git tag already on the remote so a release can't collide with a published one. It refuses to run while any `## [x.y.z]` section is an empty stub. Publish tooling only. No action required.
- Added a read-only pre-publish audit to the release script (`--mode audit`, and a gate at the start of every build mode). It verifies version/changelog agreement, no empty changelog sections, the cut version's Overview intro and its `[log]` link, that every `%key%` in `package.json` resolves in `package.nls.json`, that every `l10n('key')` in `src/` resolves in `locales/en.json`, and that no AI-authorship attribution footer leaked into a tracked file. A full publish aborts on a blocking finding. Publish tooling only. No action required.
- Modularized the largest source files into cohesive sibling modules. The shortcut runner, shortcut command registrations, run-parameters editor, favorites importers, recipe detectors, the dashboard and planner webview assets, the tree-row builder, and the activation wiring were each split along their natural seams into focused modules, with every public export preserved so dependents are unchanged. Pure structural reorganization — byte-for-byte the prior behavior, verified by a full type-check, the bundle build, and the unit suite. No user-facing change. A second pass split the Shortcuts tree provider (drag-and-drop controller and row builders extracted), the shortcut command registrations (per-shortcut config and management registrars), the action runner (the routine engine), and the activation entry (view, engine, and watcher wiring), each kept under the size cap with public exports preserved. A third pass split the shortcut store — the largest file — into a linear class chain (base persistence and accessors, then recipe/auto-shortcut detection, refresh, core mutation, field-update toggles, shortcut sets, and the concrete group layer), turning private members protected with method bodies unchanged, so no file now exceeds the size cap and the public `PinStore` API is identical.
- Hardened the release script's publish flow. Missing `VSCE_PAT` / `OVSX_PAT` tokens are now prompted for with platform-specific instructions for setting them permanently; after publishing it polls the Marketplace and Open VSX until both serve the new version (so a `vsce` exit of 0 that never propagated is caught); stale `.vsix` files are removed before packaging and the packaged filename is checked against the intended version; and new `ci-fallback` (manual release playbook) and `--quiet` options were added alongside colored output, a timing summary, and a `tsc --noEmit` type-check gate. Publish tooling only. No action required.
- Added Node-runner unit tests for the recipe-detector modules: the on-demand catalog, the ecosystem probes, the run-target block, the git-metadata reader, the scheduled-ritual detector, the routine composer, and the hygiene/process recipe builders. Each runs under `node --test` against the vscode stub with a temp-dir filesystem, covering the per-ecosystem branches, the git-remote gate, group routing, and the "scheduled rituals seed disabled" safety invariant. Tests only. No user-facing change.
- Broadened unit-test coverage across the model, import, exec, command, and i18n layers. New `node --test` files exercise the shortcut model helpers (`pinKind`, annotation detection, the empty-project-file factory and version constants), the shortcut-store internals split out of the monolith (base persistence/accessors, refresh, the mutation core and field toggles, and the shared helpers), the project-file relative-time formatter, the tapped-shortcuts recency tracker, the favorites importers (Oleg Shilo, settings, and sibling formats), the share-link encode/decode round-trip, the scheduler's run-on-startup skip/advance paths, several run-configuration command helpers, the metric setter, the tag mutator, and the `l10n` key lookup with `{token}` interpolation and missing-key fallback. Each covers the module's distinct branches and edge cases against the vscode stub, with host-dependent paths (tree items, webviews, terminals, file-system watchers) left out where the stub does not model them. Tests only. No user-facing change.

</details>

---

## [1.4.0]

See what's eating your machine and what your project is hiding: a three-tab Saropa Dashboard (processes, analytics, trends), a live toolchain monitor, workspace-bloat and empty/oversized hygiene scans, and richer scheduled reports — Sunrise project stats by language and the Saropa Lints Code Health score one click away. Boot the whole Saropa suite in one action, surface your active AI threads, and free a port wedged by a crashed dev server without leaving the editor. [log](https://github.com/saropa/saropa_workspace/blob/v1.4.0/CHANGELOG.md)

### Added

- **The process monitor is now a three-tab Saropa Dashboard — Processes, Analytics, Trends.** **Open Saropa Dashboard** (command palette) opens one webview with three tabs sharing a single strict, local-only frame (no network, no external scripts). **Processes** is the live toolchain monitor unchanged — per-tool CPU bars, a load sparkline, and a confirm-gated **End task** that names the exact PID and refuses OS/container processes. **Analytics** shows your on-device run history — most-run shortcuts, totals, this session's success/failure results, and recent runs — with a one-click **Open as Markdown preview** fallback. **Trends** charts toolchain CPU over time (a line per tool) and tech-debt markers over time from the harvest reports, and lists every dated scheduled report (lint, tests, dependencies, standup, and the rest) so you can open any of them in a click. Each tab degrades to its underlying surface — the snapshot command, the Markdown summary, the report files on disk — so nothing is all-or-nothing. **Open Toolchain Monitor** still works and opens the Processes tab.
- **See which tool in your stack is the CPU hog — the toolchain monitor.** The Recipes group gains **Open the toolchain monitor**, a live panel (the new Saropa Dashboard) that shows only your detected toolchain's processes — editor, language servers, AI agents, dev servers, shells — grouped per tool with a live CPU bar and total RAM, sorted so the heaviest leads. The CPU figure is a two-sample live delta (load right now), not the misleading cumulative CPU time a single snapshot reports. Expand a tool to see its individual processes; end a single runaway with a confirm-gated **End task** that names the exact process and PID (OS and container rows are never killable). **Snapshot the toolchain** now writes that same grouped, live-CPU table to a dated report under `reports/` and opens it, to attach to a bug or a slow-machine message. An optional **heartbeat** (`saropaWorkspace.processMonitor.heartbeat.enabled`, off by default) samples on a timer into `reports/process-trend.csv` and warns only when a tool crosses a RAM or process-count ceiling — the leaked-server and helper-swarm cases — and feeds the panel's load sparkline. Reading the process table is harmless; nothing is transmitted, and a process is only ever ended by an explicit, named action.
- **Snapshot your machine's processes to a dated report.** A new **Process Monitor** group adds **Snapshot the toolchain**, which captures the current OS process table (memory and CPU time) to a dated file under `reports/` and opens it — a shareable record to attach to a bug or a slow-machine report. Works on Windows, macOS, and Linux.
- **Catch the project that freezes VS Code on open — the workspace bloat scan.** VS Code crawls your whole workspace on folder-open except a short default list (node_modules, .git), so any directory that grows large and isn't in `files.watcherExclude` makes the file watcher pin a CPU core and hang the window — test caches, build outputs, generated blobs. The new **Workspace bloat scan** (a scheduled recipe, daily 04:45, disabled until promoted, or run on demand) measures each immediate child directory and flags any past a size or file-count ceiling (defaults 1 GB / 50,000 files) that VS Code would crawl, plus any project that depends on the `@vscode/test-(electron|cli)` downloader but doesn't exclude `**/.vscode-test/**` (that cache grows without bound — one real project reached 16 GB). It writes a dated `reports/<stamp>_workspace_hygiene.md` with the exact `files.watcherExclude` line to add, opening it only when something crosses a ceiling. Two one-click fixes for the open project: **Guard this project** merges the watcher excludes into `.vscode/settings.json`, and **Prune .vscode-test** deletes the cache after a confirm that names the size reclaimed. A `saropaWorkspace.hygiene.roots` setting extends the scan across sibling projects (report-only for projects other than the open one). Ceilings are configurable under `saropaWorkspace.hygiene`.
- **Find the empty and oversized files cluttering your project — hygiene scan.** A new **Scan for Empty & Oversized Files** recipe (in the Workspace group, or the command palette) recursively crawls the project and reports the outliers at the extremes: empty (zero-byte files, zero-child folders) and oversized (files and folders past a size ceiling). It writes a structured, dated `reports/<date>/<time>_filereport.json` you can diff run-to-run, and raises a sticky notification naming the issue count with an **Open report** action (a clean scan reports "no issues" and still writes the report). Mode (`empty` / `oversized` / `both`), the size ceilings, an optional under-size floor, `.gitignore` handling, and extra exclude globs are all configurable under `saropaWorkspace.hygiene.*`; a built-in ignore set keeps the crawl out of `node_modules`, `.git`, and build output, and symlinked folders are never followed.
- **Save a hygiene scan with its own scope and thresholds.** Beyond the one settings-driven whole-project scan, **New Hygiene Scan** (Shortcuts title menu `···`, or the command palette) walks you through a scope folder, what to report (empty / oversized / both), and the size ceilings, then saves it as a reusable shortcut with an auto-generated name like *Hygiene: src (oversized, files >50MB)*. Double-click it to re-run that exact scan, or give it a schedule — so you can keep several targeted scans (a strict one for `assets/`, a loose one for the whole repo) side by side.
- **Read the exact Saropa Lints Code Health score — no report to open.** With the Saropa Lints extension installed, **Show Saropa Lints Code Health Score** (the Saropa Suite group, or the command palette) reads the Lints public API directly and reports the precise 0–100 score with its error / warning / info breakdown and a one-click path to the full Code Health dashboard. If no analysis has been run yet it offers to run one first; if only a partial sweep exists it says so rather than showing a misleading number.
- **A richer Sunrise project stats report — your codebase by language.** The scheduled **Sunrise project stats** ritual now writes a per-language breakdown of the tracked codebase — files, lines, and each language's share of the total — alongside the recent commits and contributor shortlog, all in one dated report it opens for you. Tracked files come from `git ls-files` (so ignored paths are skipped for free) and the aggregation runs cross-platform, with a file cap, a per-file size cap, and binary-file skipping so a large repo never freezes the run.
- **Boot the whole Saropa suite in one action.** When two or more Saropa Suite tools are detected (Saropa Lints, Drift Advisor, Log Capture), a new **Boot the Saropa suite** recipe runs each tool's start action in sequence — open the Drift inspector, run a lint analysis, open a capture log — for whichever tools are installed.
- **See which shortcuts earn their place — View Run Analytics.** A new **View Run Analytics** action (the Shortcuts view title menu, the Recent group's right-click menu, or the command palette) opens a read-only summary of your local run history: total runs, your most-run shortcuts, this session's success/failure results, and recent run times. It reads only the on-device run history — nothing is transmitted — and respects the same controls as the rest of it: turn run history off and there's nothing to show, and **Reset Run History** clears it.
- **More recipes, detected from your project.** The recipe catalog gains everyday actions when the markers are present: **Format code** (prettier / `dart format` / `cargo fmt` / `gofmt` / ruff or black), **Clean build artifacts** (flutter / cargo / go, or a `clean` package script), and **Upgrade dependencies** (npm / pub / cargo / go) join the run group; **Open commit history** for the current branch joins the open group alongside the existing branch and pull-request links; and one-click **Open the README / CHANGELOG / LICENSE / contributing guide** appear whenever those files exist at the project root. Like every recipe, each is only offered when its marker is detected, and remove one to keep it from re-appearing.
- **Each detected recipe now explains what it does and where it came from.** Clicking a recipe (open-on-GitHub, run-tests, a scheduled ritual, a Saropa Suite action, and the rest) opens a detail describing what running it would do and which project file it was detected from; the same text appears on hover. The explanation carries over if you promote the recipe to a saved shortcut.
- **Your active AI conversations, saved where you left them.** Saropa Workspace now scans your project's chat folders (`.claude`, `.cline/tasks`, `docs/chats` by default, configurable) and surfaces the most recently touched Claude/AI threads in a new **Active AI Threads** group — so the conversation where you were refactoring a component is one click away instead of a hunt through a wall of identically titled tabs. A thread opens its chat on claude.ai when the transcript carries a link or id, or opens the local transcript file otherwise. Only the freshest ten are shown; remove one and it stays gone (restore them with **Restore Recipes**). The group also offers a **Start a new Claude chat** shortcut whenever a chat folder is present. Turn the scan off entirely with `saropaWorkspace.aiContext.enabled`.
- **Free a wedged port without leaving the editor — the "port blocked" savior.** When a background shortcut fails to start because a port is already in use — a zombie dev server from a crashed run still holding `:3000` — the failure toast now reads the port from the run's own output, finds the process holding it, and offers **Kill process & retry**. It names the exact process and PID (e.g. *Port 3000 is held by node.exe (PID 4512)*), and the kill is gated behind a modal confirm that names the same PID and image before anything is stopped — never an automatic kill, and it refuses to touch the OS/init processes or VS Code's own host. On a confirmed kill it frees the port and re-runs the shortcut in one click. When the holding process can't be identified, the toast instead names the port and offers **Inspect Port**, opening a terminal pre-filled with the lookup command so you can free it by hand. Detection covers the common phrasings (Node `EADDRINUSE`, dotnet bind failures, `address already in use`) across Windows (`netstat`/`tasklist`), macOS, and Linux (`lsof`/`ps`); it applies to background runs only, since terminal and external-window runs don't capture output to read the port from.

---

## [1.3.0]

Tame a long shortcut list: filter by text and by Scripts/Files/Failed chips, tag shortcuts into focus modes, scope shortcuts to a git branch, keep several named shortcut sets per workspace, and section the tree with comments and separators. Shortcuts can now show a live file metric, the last lint/test result, an expiry countdown, and a run count — and a tab you've kept pinned for hours gets offered as a durable shortcut. [log](https://github.com/saropa/saropa_workspace/blob/v1.3.0/CHANGELOG.md)

### Added

- **Find a shortcut instantly — filter the Shortcuts view by text and chips.** A new **Filter Shortcuts** button on the Shortcuts toolbar opens a find bar: type and the tree collapses to matching shortcuts as you go (matching a shortcut's name, path, or command), hiding any group that has no match. Three buttons in the bar narrow by kind and state — **Scripts** (action shortcuts), **Files** (file shortcuts), and **Failed** (shortcuts whose last run this session failed) — and combine with the text. While a filter is active the toolbar shows a filled filter icon and a one-click **Clear Shortcut Filter**, and a line under the view title always names what's filtered and how many shortcuts are hidden, so a narrowed tree never reads as lost shortcuts. The filter is remembered per workspace.
- **Tag your shortcuts and focus the view on one "mode" at a time.** Right-click a shortcut and choose **Tag Shortcut** to give it freeform tags like `ops`, `dev`, or `review` — reuse existing tags from the list or add new ones — shown as `#ops` chips on the row and in the hover. The Shortcuts toolbar's **Filter Shortcuts by Tag (Mode)** button then lets you pick one tag: the tree collapses to just those shortcuts and the groups that hold them, hiding everything else; choose **Show all shortcuts**, **Clear Tag Filter**, or the toolbar's clear button to restore. The active mode is always named in the filter banner with a count of what's hidden, so a focused tree never reads as lost shortcuts. Tags share the same filter as text search and the Scripts / Files / Failed chips, so a tag mode composes with them. (This filters the Saropa Shortcuts view only — distinct from **Focus on Saved Files**, which hides non-saved files in the Explorer.)
- **Link a shortcut to a git branch — the context time-machine.** A shortcut can now be scoped to the branch you're working on: right-click it and choose **Link to Current Branch / Show on All Branches**, and it shows in the Shortcuts view only while that branch is checked out. Switch branches and the tree re-filters live — your `feature/auth` refactor shortcuts disappear when you jump to `main` for a hotfix, and reappear the moment you switch back. A linked shortcut wears an `on <branch>` chip so it always reads as branch-scoped, and unlinked shortcuts (the default) keep showing on every branch. Nothing is ever silently lost: when a branch can't be read the shortcut stays visible, and a **Show Shortcuts from All Branches** toolbar button appears whenever branch filtering is hiding something — one click reveals every branch-linked shortcut (the escape hatch for a shortcut tied to a deleted branch), and **Filter Shortcuts by Current Branch** switches back. Branch detection reads `.git/HEAD` directly (no `git` process, no dependency) and the choice is remembered per workspace.
- **Keep separate sets of shortcuts per workspace and switch between them in a click — multiple favorite sets.** A workspace can now hold several named **shortcut sets**, each its own collection of project shortcuts and groups, with exactly one active at a time. A new **shortcut set switcher** in the status bar shows the active set's name; click it to switch sets or to create, rename, duplicate, or delete one — and the same actions live in the Shortcuts toolbar `···` menu (**Switch / New / Rename / Duplicate / Delete Shortcut Set**). Switching repaints the tree to the chosen set's shortcuts instantly. **Global shortcuts are shared across every set** (a global favorite is cross-workspace by definition), so only your project shortcuts change as you switch. Creating a set starts its project shortcuts empty; duplicating copies the current set's shortcuts and groups into an independent copy and switches to it. Every action names the set it acted on, and deleting a set asks first (it removes that set's project shortcuts; global shortcuts are untouched, and you can never delete the last set). Existing shortcuts migrate untouched into a starter set named **Default** — single-set behavior is unchanged and the switcher stays hidden until you create a second set.
- **Label and divide a long shortcut list with comments and separators.** Two new non-runnable entries organize a crowded Shortcuts view: a **comment** (a text note like *Deploy scripts* or *Daily checks*) and a **separator** (a horizontal divider line). Add either from the Shortcuts toolbar `···` menu (**Add Comment...** / **Add Separator**) to append it, or from a shortcut's right-click menu to drop it right after that shortcut. They drag and reorder like any shortcut, so you can section your list however you like, and they round-trip through shortcut-set export/import. Neither ever runs or opens — a click does nothing, they carry no run badge, and they never appear in the Run Shortcut palette or a "top shortcut" keybinding. Rename a comment from its right-click menu; remove either with Remove.
- **Give a shortcut an expiry date — it removes itself when its time is up.** A new **Shortcut Expiry (Time-Bomb)** submenu on a shortcut's right-click menu lets you set a shortcut to auto-remove: **Keep Until...** picks a moment (in 1 hour, end of today / tomorrow / Friday, or a custom date and time), and **Keep Until Branch Changes** ties the shortcut to your current git branch so it clears the moment you check out a different one. A bombed shortcut wears a watch glyph and shows its countdown (`2h left`) on the row, with the exact deadline in the hover; **Clear Expiry** defuses it. When a shortcut expires it is removed quietly with a single toast that names what went and offers **Undo** — and only shortcuts you explicitly time-bombed ever auto-remove, so a normal shortcut is never touched. The cure for adding `db_migration_v42.sql` as a shortcut for today and finding it still there six months later.
- **Watch a file's size, lines, or last-change live on its shortcut — and get a toast when it grows too big.** A file shortcut can now carry a **live metric** badge: its **file size** (`bundle.js · 245 KB`), **line count**, or **last modified** time, refreshed on the shortcut as the file changes on disk — so you can watch a bundle shrink, a Docker context grow, or a dump file fill without switching to a terminal and typing `ls -lh`. Set it from a shortcut's right-click menu (**Set Live Metric...**), and for the size metric give it an optional limit (`250kb`, `5mb`, `1gb`): the badge turns to a warning and a one-time toast names the file and its new size the moment it crosses that limit, then stays quiet until it crosses again. It is opt-in per shortcut, so it costs nothing until you ask for it — each metric'd shortcut gets a single narrow file watcher (no polling), change bursts are debounced into one read, and line-counting a very large file falls back to showing its size so a multi-GB file is never read into memory.
- **The Project Files and Recipes views show how many items they found.** The Project Files section now carries a count next to its title — the total number of surfaced files (README, CHANGELOG, manifests, and the rest) across your open folders — and the Recipes section likewise shows the total number of detected recipes, so you can see at a glance how many there are without expanding either. Both counts update live as files are saved, your file list changes, or recipes are re-detected, and an empty (or disabled) view shows no number rather than a "0".
- **See what the last lint sweep or test run found, right on the shortcut.** A shortcut that runs a linter / analyzer or a test suite now badges itself with the result of its last run: a compact `3✖ 5⚠ 2ⓘ` (errors / warnings / info) for a lint sweep, `12✓ 1✗` for a test run, and `✓` when a re-run comes back clean — so the dawn lint sweep and test-trend rituals tell you the outcome without opening the report. The full breakdown is in the hover. Counts are parsed from the run's own output (Dart/Flutter analyze, ESLint, tsc; Dart/Flutter test, Jest, vitest, mocha, pytest, cargo test) and kept on this machine for the session only.
- **Focus the Explorer on just your saved files.** A new **Focus on Saved Files** action (Shortcuts title menu `···`, or the command palette) drives VS Code's `files.exclude` to hide everything in the Explorer except your saved files and the folders that lead to them — a favorites-only workspace view. It walks only the directories on the way to a favorite, so a huge tree stays fast, and your own existing file filters are preserved and restored exactly when you choose **Exit Focus on Saved Files**. The toggle survives a window reload, and folders with no saved files inside them are left untouched.
- **A badge on the activity-bar icon counts shortcuts you haven't used yet.** The Saropa Workspace icon shows the number of saved items you have not yet opened or run, so newly added shortcuts are easy to notice. Opening or running a shortcut clears it from the count, and the badge disappears once you've used everything (it never shows a zero).
- **A shortcut's hover now shows how many times you've run it.** Each shortcut's tooltip includes its lifetime run count (kept on this machine only, alongside the Recent list), so the shortcuts that earn their place are easy to spot. The count is hidden when run history is turned off.
- **Follow a log file live — tail -f inside the editor.** Right-click a file shortcut → **Toggle Log Follow (tail -f)**. With follow on, opening that shortcut scrolls to the end of the file and keeps it pinned to the newest lines as the file grows on disk, so a running process's log stays at the bottom without you closing and reopening the tab. Turning it on offers to open the file right away; turning it off stops the auto-scroll immediately. Applies to file shortcuts only, and the follow lives only while the tab is open.
- **Saropa offers to save a tab you've kept pinned for hours.** When you pin an editor tab (right-click the tab, Pin) and leave it pinned past a threshold — 2 hours by default — Saropa Workspace offers to add that file as a shortcut, either **Add to workspace** (shareable via the repo) or **Add globally**. A manually pinned tab is a strong "this file matters" signal, so this turns it into a one-tap durable shortcut. The timer is tracked on this machine only and never transmitted; a tab pinned before the window opened starts counting from open (so it's never offered on data we can't date). Each file is offered at most once, and **Don't ask again** suppresses it permanently — reversible with the **Restore Pinned-Tab Suggestions** command. Tune the wait with `saropaWorkspace.suggestPinnedTab.afterHours`, or turn the whole prompt off with `saropaWorkspace.suggestPinnedTab.enabled`.

### Changed

- **The Project Files section now starts collapsed.** Project Files opens collapsed so the Shortcuts and Recipes views stay front-and-center when the sidebar loads; expand it when you want it. Because it's collapsed, its file scan is also deferred until the first time you open it — nothing is read from disk for that view until you ask for it.
- **The Recent group now starts collapsed.** Your last-run shortcuts stay one click away under **Recent**, but the group no longer opens expanded and pushes your own Project and Global shortcuts down the view. Expand it once and it stays open the way you left it.

---

## [1.2.0]

Shortcuts can now run themselves and each other: chain one shortcut after another or after a build, publish, or git commit/push; schedule with days-of-week, every-N-hours/days, cron, or run-on-workspace-open; run on save or when you step away; and compose recipes into routines, with a Morning routine that runs itself. Pause a shortcut without losing its schedule, run only one instance at a time, and shape it all in a new visual Schedule & Workflow Planner. [log](https://github.com/saropa/saropa_workspace/blob/v1.2.0/CHANGELOG.md)

### Added

- **Chain shortcuts so one runs after another — and after build, publish, or git.** A new **Configure Triggers** action (a shortcut's right-click menu, the `2_edit` group) sets what auto-runs a shortcut beyond its schedule: **run after a shortcut** (optionally only when that shortcut succeeded), or **run after an event** — *build*, *publish*, *git commit*, or *git push*. "Run X after Y" and "run Z after Y" are independent links, so one finished job can fan out to several. *build* / *publish* fire when a shortcut you mark (its **emits** setting) as that kind of step finishes; *git commit* / *git push* are detected straight from the repo's `.git` logs (no `git` process spawned), so a post-commit job just works. The chain engine guards against loops with a short per-shortcut cooldown, and every auto-run is logged in the output channel as an audit trail. A failed source skips its post-steps; an untracked run (terminal / external) chains on dispatch.
- **Flexible schedules — days of the week, and every N hours or days.** **Configure Schedule** gains a **Days of week** field: pick the weekdays a daily time runs on (with one-tap **Weekdays** and **Weekends** shortcuts; empty means every day), so "weekdays at 9am" is a first-class schedule. The repeat interval is no longer minutes-only — a custom interval now asks for a unit (minutes / hours / days), so "every 3 days" is three taps instead of arithmetic. Existing schedules are unchanged.
- **Schedule a shortcut with cron, build it without typing cron, or run it on workspace open.** A shortcut's schedule now takes a full **5-field cron expression** (minute hour day-of-month month day-of-week — standard syntax with lists, ranges, steps, and 3-letter month/day names), computed by the same next-fire engine as the daily time and repeat interval, so the soonest of them all wins. You almost never type raw cron: **Configure Schedule** gains a **Cron schedule** builder that composes the common ones for you — *every weekday at a time*, *every day at a time*, *a weekday each week*, *the 1st of each month*, *every few minutes during work hours (Mon–Fri 9–5)*, *every hour on the hour* — and an **Advanced** path validates a hand-typed expression live before it can be saved. A new **Run on workspace open** toggle fires the shortcut once each time you open the workspace, shortly after the window settles, de-duplicated so a window reload does not re-run it. The cron parser is in-repo and dependency-free.
- **Run a shortcut automatically when its file is saved.** **Configure Run** gains a **Run on save** toggle: turn it on for a script shortcut and Saropa Workspace runs it every time you save that file — the Code-Runner "run on save" convenience, scoped to the exact shortcut you choose. Off by default, so a save never triggers an unexpected run; it applies only to a runnable file shortcut (a plain file with no run command, or an action shortcut, has nothing to run on save).
- **Run a heavy shortcut while you step away — the coffee-break runner.** A shortcut can now carry a **Run when idle** trigger: after a set number of minutes with no VS Code interaction (focus, cursor movement, switching editors), the shortcut runs once in the background, so a slow pre-push check or integration suite fires while you are away from the keyboard instead of stalling your typing. Add it from **Configure Triggers → Run when idle...** (default 3 minutes). It always runs in the background channel — never the terminal or a pop-out window — and badges its result on the shortcut, so a finished run is waiting when you return. It fires at most once per idle stretch and re-arms only after your next burst of activity; a shortcut that needs interactive input is skipped (an unattended run cannot answer a prompt). "Idle" means no VS Code interaction — it does not track mouse movement elsewhere on your machine.
- **Run a block of recipes back-to-back — routines, and a Morning routine that runs itself.** A new **routine** shortcut is a recipe of recipes: an ordered set of your existing recipe shortcuts that run one after another as a single action. Select two or more shortcuts (Ctrl/Cmd-click), right-click, and choose **New Routine from Selection** to compose them; or let the **Morning routine** be auto-offered when a project already has two or more morning rituals (it pre-fills, in run order, the workspace bloat scan, dawn lint sweep, sunrise project stats, standup digest, and PR review queue). Members run strictly in sequence (so report-writing rituals don't overlap and spike the CPU), continue-on-failure — one broken member never blocks the rest — and the routine writes a one-screen summary linking each member's outcome, opening it only when something needs you and badging itself red if any member failed. The Morning routine carries one schedule (daily 08:00, disabled until you promote it), so a single timer drives the whole block; **Run now** fires it on demand. Routines don't nest, and an interactive member is skipped in an unattended (scheduled) run.
- **Pause a shortcut to stop it running on its own — without losing its schedule.** Right-click any shortcut and choose **Pause** to suspend every automatic run: the scheduler stops firing it, chained triggers (after another shortcut, after a build/publish/commit/push) and run-on-idle ignore it, it emits no events of its own, and run-on-save no longer launches it. Its schedule and triggers stay exactly as configured, so **Unpause** resumes right where it left off — pausing is "stop running this by itself," not "delete the automation." A paused shortcut still runs the moment you click it (or pick **Run**), so a manual run is always one tap away. The row reads `paused` with a muted icon and a hover that explains what's suspended, and the menu swaps **Pause** for **Unpause** depending on the shortcut's state.
- **A shortcut won't run on top of itself — one run at a time by default.** Every shortcut is now single-instance: while one of its runs is still in progress, a scheduled slot, a chained trigger, or a run-on-save is skipped (and logged) rather than starting a second — so an hourly job that hangs no longer stacks up copies of itself. Click a shortcut that's already running and Saropa asks first: **Stop and re-run**, **Run anyway**, or **Show output**. The guard sees the runs Saropa tracks (background and report runs); a run sent to the integrated terminal or an external window is fire-and-forget and can't be tracked, so use a **cross-process lock** for those. Both settings live in **Configure Run**: switch **Concurrent runs** to *Allow* to let a shortcut overlap, or set a **Cross-process lock** name. A lock name is shared across VS Code windows, terminals, and any script that honors the same convention (the lock file lives in your system temp folder), so a heavy job guarded by a name like `nllb-gpu` refuses to start while a live holder owns it anywhere on the machine — and a crashed holder is detected and cleared automatically, so a lock never wedges a shortcut permanently.
- **Restore your working context on open — a workspace boot sequence.** Define an ordered set of shortcuts (open the key files, start the dev server) that runs when the workspace opens. **Configure Workspace Boot Sequence** (Shortcuts title menu `···`, or the command palette) lets you add shortcuts, reorder or remove each step, toggle whether the sequence runs on open, and choose whether a failed step stops the rest. On open you're asked once before anything runs — declining skips it for the session (nothing ever runs silently) — and each step shows its own outcome, with a per-step log in the output channel. Run it any time with **Run Workspace Boot Sequence**. A step whose shortcut was since removed is skipped, never blocking the rest.
- **See and shape your automation visually — the Schedule & Workflow Planner.** A new **Open Schedule & Workflow Planner** button (the Shortcuts view toolbar, or the command palette) opens a panel with three linked views. **Day** plots every daily-scheduled shortcut on a 24-hour ruler with a live "now" line, and lists the repeating-interval shortcuts as cadence chips. **Week** is a 7-day × 24-hour calendar where each scheduled shortcut is a **draggable block** — drag it up or down to retime (snaps to 15 minutes), or across to another weekday to move it; the schedule is rewritten and the scheduler re-armed live. **Workflow** is a node graph of your shortcuts and the events they chain off: drag a shortcut's handle onto another shortcut to chain them, drag an event from the **toolbox** onto a shortcut to run it after that event, right-click a node for run / schedule / triggers / remove, or right-click the canvas for a searchable link builder with autocomplete. Node positions are remembered. The panel is local-only (strict CSP, no network), themed to your editor, and every action routes through the same run and store paths the tree uses.
- **Make one shortcut wait for another to succeed.** In **Configure Run** a new **Depends on** field lets a shortcut name a prerequisite — say *Deploy* depends on *Build*. Until *Build* has run successfully in the current session, *Deploy* shows a lock icon and a "waiting on Build" note, and trying to run it is blocked with a one-click offer to run *Build* first. The dependency is session-scoped (a fresh window starts with nothing satisfied, so a stale build can't be deployed), and a prerequisite that's since been deleted never leaves a shortcut permanently locked.
- **Edit the shortcuts file by hand, see the tree update live.** A new **Edit Shortcuts Config (JSON)** action (Shortcuts title menu `···`, or the command palette) opens the raw `.vscode/saropa-workspace.json` for direct editing — the power-user path alongside the GUI editors. Save your edits and the Shortcuts view refreshes immediately, no reload. With several workspace folders open you pick which folder's config to edit, and the file is created (empty and valid) if it doesn't exist yet.

### Changed

- **A scheduled shortcut's run action now reads "Run now".** On a shortcut that carries a schedule, the right-click **Run** is relabeled **Run now**, so firing the job ahead of its next timer — to test it or get a fresh result — reads as intentional rather than as a generic run. It runs exactly the same way; only the label changes.

---

## [1.1.0]

Bring your existing favorites with you and manage saved files in place: import from Bookmarks, Favorites Panel, and two more favorites extensions (sections and dividers included); add files anywhere as shortcuts — external folders, remote/virtual filesystems, or a single line in a huge file; and create, duplicate, rename, copy, lock, or template a saved file without leaving the Shortcuts view. Export and import whole shortcut sets as a file. [log](https://github.com/saropa/saropa_workspace/blob/v1.1.0/CHANGELOG.md)

### Added

- **Import from two more favorites extensions — Bookmarks and Favorites Panel.** **Import Favorites** now also brings in alefragnani **Bookmarks** (`.vscode/bookmarks.json`, when saved in the project): every line-level mark becomes a line shortcut that jumps back to that exact line, keeping its label. It also imports sabitovvt **Favorites Panel** items from your settings: a file entry becomes a file shortcut, a URL a url shortcut, a VS Code command a command shortcut, a program/terminal line a shell shortcut, and a multi-step sequence a macro — carrying each item's icon and color. Re-running the import never duplicates what's already saved, and any entry with no shortcut equivalent (an insert-code action, a project-switch folder) is listed in the output channel and skipped rather than aborting the import.
- **Keep your favorites' sections when you import them — comments and dividers come across too.** Importing an oleg-shilo **Favorites Manager** list now carries its sectioning into the Shortcuts view: a `#` comment line becomes a comment shortcut (the text after the `#`) and a blank-line divider becomes a separator, landing in the same order among the file shortcuts they sit between. Runs of blank lines collapse to a single divider and a stray leading or trailing blank is ignored, so the file's own formatting never leaks extra dividers. Your file shortcuts still import once and only once; the section labels and dividers are positional, so each import lays them out exactly as the source file has them.
- **Import favorites from two more extensions.** **Import Favorites from Other Extensions** now also reads oleg-shilo's *Favorites Manager* text lists (`.vscode/fav.local.list.txt` or `.fav/local.list.txt`, one `path` or `path|alias` per line, with `#` comments — an alias becomes the shortcut's name) and howardzuo's `favorites.resources` setting, alongside the existing kdcro101 `.favorites.json`. Import stays idempotent (running it twice adds no duplicates), and any entry it can't add as a shortcut — a folder/group entry, a blank or malformed line, an unparseable file — is now listed in the output channel and skipped rather than silently dropped, with a one-click **Show Output** on the result when anything was skipped.
- **Add any file on disk as a shortcut, even outside this workspace.** A new **Add External File…** action (the Shortcuts view title menu, or the command palette) opens a file picker and adds whatever you choose as a global shortcut — so a schema or config in another local repo is one click away without opening a second VS Code window.
- **Add files on remote and virtual filesystems as shortcuts, not just local ones.** Global shortcuts now keep the full resource URI when you add a file that lives on a non-local filesystem — a Remote-SSH / WSL / dev-container host, or a virtual file provider — so opening the shortcut reaches the right machine instead of a wrong local path. **Add External File...** and **Add Active File as Shortcut (Global)** in a remote window add the remote file correctly, and the shortcut opens, peeks, reveals, and round-trips through export/import like any other. Local shortcuts are unchanged (still stored as a plain path), and shortcuts saved by earlier versions keep working. (Running a script on a remote host is unchanged — this covers adding and opening.)
- **Turn a file shortcut into a template.** Right-click a file shortcut and choose **Use as Template…**, type a new name, and Saropa Workspace duplicates the file with a casing-aware rename of the file's base name throughout — `base_controller.ts` with `BaseController`/`base_controller`/`BASE_CONTROLLER` becomes `user_account.ts` with `UserAccount`/`user_account`/`USER_ACCOUNT` — then opens the copy. The original is untouched, and an existing file is never overwritten.
- **Manage saved files without leaving the Shortcuts view — create, duplicate, rename, copy, delete.** A file shortcut's right-click menu (the new `4_file` group) now carries five lightweight file-manager actions: **New File Here** creates a file next to the shortcut and adds it; **Duplicate File** makes a byte-for-byte copy (`report.md` → `report copy.md`) and adds it; **Rename File on Disk** renames the file and follows it with the shortcut, so its run config, schedule, and icon survive; **Copy File To...** copies it into a folder you pick; and **Delete File** moves it to the system trash (recoverable) after a confirm, then offers to remove the shortcut. Each names the file it acted on, never overwrites an existing file, and works on file shortcuts only.
- **Lock a saved file read-only from the tree.** Right-click a file shortcut and choose **Lock / Unlock File (Read-only)** to flip the file's read-only attribute on disk — the "stop me (or a script) from clobbering this by accident" guard, without leaving the Shortcuts view. It toggles the real filesystem attribute (not a VS Code-only flag), so a locked file is read-only everywhere; the confirmation names the file and tells you which state it's now in.
- **A shortcut whose file was deleted is now flagged, and clicking it offers a fix.** When a saved file no longer exists on disk, the shortcut shows a warning icon and a "file not found" hover instead of looking normal. Clicking it (to open or run) no longer hits a cryptic "cannot open file" error — instead a message names the shortcut and lets you **Relocate...** it to the moved or renamed file (keeping its run settings, schedule, and icon), **Remove** it, or **Show in Folder** to go find it. Shortcuts are never removed automatically, since a missing file is often temporary (a branch switch or a regenerated build artifact).
- **Add a shortcut to a specific line in a big file.** Put your cursor on the function you keep coming back to in a 3000-line file, right-click → **Add This Line as Shortcut** (or run it from the command palette). Opening that shortcut later jumps straight to the line and flashes it, instead of dropping you at the top of the file to scroll. The same file can have shortcuts to several different lines. The target is line-based, so heavy edits above it can shift it; it's always clamped to a valid line so it never points past the end.
- **Extensionless `#!` scripts now run through the interpreter they declare.** When a shortcut has no command prefix set and its file type has no default interpreter, Saropa Workspace reads the file's shebang (`#!/usr/bin/env python3`, `#!/bin/bash`, …) and runs it through that interpreter instead of relying on the file's executable bit — the same way Code Runner handles a *nix script with no extension. A file with no shebang still runs directly, as before.
- **Share a whole shortcut set as a file — export and import.** **Export Shortcuts to File** (Shortcuts title menu `···`, or the command palette) writes your shortcuts and their groups to a versioned `.json` you can commit or send to a teammate; choose project shortcuts, global shortcuts, or both. **Import Shortcuts from File** reads it back in — additive and idempotent, so it never overwrites your existing shortcuts: a shortcut already present (a file shortcut by path, an action shortcut by name and kind) is skipped, and groups are recreated (reusing a same-named group rather than duplicating it). Importing never runs a shortcut. Round-trips the full set — groups, run config, and icons.

### Changed

- **The Global Shortcuts header is hidden until you have a global shortcut.** An empty **Global Shortcuts** row no longer sits in the tree showing "0" — it appears the moment you create your first global shortcut and disappears again if you remove them all. Project Shortcuts always shows, since it's where a new shortcut lands.
- **Shortcut rows are easier to scan.** A shortcut's row no longer crowds its branch and tag chips inline alongside the run state, path, and metric — those two move to the hover, where they already had their own line — so the row leads with what's actionable (run state, the path or action, and any live metric) instead of a long dotted string that truncated unpredictably in the narrow sidebar. Hover any shortcut to see its linked branch and tags.
- **The Shortcuts toolbar shows fewer, clearer buttons.** The title bar now carries four everyday actions — filter, filter by tag, run a shortcut, and refresh (plus the branch toggles when they apply) — and moves the less-frequent ones into the `···` overflow menu: **Open Schedule & Workflow Planner**, **New Group**, and **Restore Auto-Shortcuts**. Everything is still one menu away, and the toolbar reads at a glance.
- **Saropa tells you how shortcuts work, once.** Every shortcut's hover now ends with a reminder — *Single-click opens · double-click runs* — and the first time you have a shortcut, a one-time tip states the same, so the core gesture is never a mystery (especially if you added the shortcut from the editor or Explorer menu and skipped the empty-view hint). The tip shows at most once, ever.

### Fixed

- **Faster startup.** The activity-bar view no longer runs two whole-workspace file searches every time the window opens. Auto-shortcut patterns that name an exact file (the defaults `pubspec.yaml` and `analysis_options.yaml`) are now resolved with a direct file check instead of a project-wide search, so a project that has neither no longer pays a search cost on launch.

---

## [1.0.1]

Run a script in its own pop-out terminal window — optionally as administrator.

[log](https://github.com/saropa/saropa_workspace/blob/v1.0.1/CHANGELOG.md)

### Changed

- **The "Workspace Shortcut" menu now shows only the valid action.** Right-clicking a file (in the Explorer, the editor, an editor tab, or a shortcut row) shows **Add to Project Shortcuts** only when it is not already a project shortcut, and **Remove from Project Shortcuts** only when it is — and likewise for global shortcuts. The menu reflects the exact file acted on, not whichever editor is focused.

### Added

- **Auto-shortcuts can be dragged into folders.** Auto-detected shortcuts (the ones from `autoPins.patterns`, like `pubspec.yaml` and `analysis_options.yaml`, plus the "Workspace config" example shortcut) can now be dragged into and out of project folders, the same as explicit shortcuts. Their folder is remembered across reloads; deleting a folder moves its auto-shortcuts back to the top level.
- **New external window** run location. In **Configure Run -> Run in**, a shortcut can now launch in a separate OS terminal window outside VS Code (alongside the existing integrated-terminal and background-channel choices). The window stays open after the script finishes so its output is readable.
- **Administrator privileges** for an external window. When **Run in** is set to the new external window, an **Administrator privileges** toggle requests an elevated window (Windows UAC prompt; `pkexec`/`sudo` best-effort on Linux and macOS). Per-shortcut environment variables are not passed into an elevated window,  which is surfaced when a run drops them.

---

## [1.0.0]

Initial release featuring customizable file shortcuts, automated project recipes, and structured local script execution.

[log](https://github.com/saropa/saropa_workspace/blob/v1.0.0/CHANGELOG.md)

### Added

- **Initial Release of Saropa Workspace** core functionality, providing seamless script and file shortcuts within VS Code workspace environments.
- **Dedicated Recipes View:** A standalone **Recipes** section in the sidebar, separate from the Shortcuts view. Auto-detected shortcuts (open on GitHub, run scripts, Saropa Suite tools) live in their own view so they never bury manual shortcuts, with **Restore Recipes** accessible in the view's title bar. Per-recipe actions include run, stop, show output, promote to shortcut, and copy path.
- **Submenu Shortcut Management:** Per-scope **Remove from Project Shortcuts** / **Remove from Global Shortcuts** actions are cleanly located inside the **Workspace Shortcut** submenu on a shortcut's context menu.
- **Extension Branding:** Brand-new extension and activity-bar icon designed as a clean, flat referee-whistle silhouette on the warm Saropa Suite tile, matching the Suite and Log Capture icons. 
- **Comprehensive Documentation:** Full README detailing all shipped capabilities—recipes, shortcut groups with drag-and-drop, the Run Shortcut palette and overrides, keybindings, smart shortcut suggestions, run-target inference, the next-run status bar, stop/force-kill, and last-run status. A forward-looking `ROADMAP.md` focuses exclusively on future phases.
- **Project Files Shortcuts:** Add and remove shortcuts directly from the **Project Files** view via an inline add/remove toggle. Each row shows a "saved" tag when the file is already a shortcut in the project, updating dynamically as shortcuts change.
- **Workspace Shortcut Context Submenu:** Explicit add/remove actions for both scopes—Add/Remove to Project Shortcuts and Add/Remove to Global Shortcuts—available from the Explorer, editor tabs, editor body, file-shortcut rows in the Shortcuts view, and Project Files rows. Right-clicking an editor tab targets that specific file, while the in-body menu and command palette act on the active editor.
- **Recent Group and Local Run Telemetry:** A **Recent** group at the top of the sidebar lists recently run shortcuts across both scopes, displaying execution recency and a "(scheduled)" tag for unattended triggers. Single-click opens details while double-click or the play button re-runs. Powered by on-device, private local history that can be toggled off via `saropaWorkspace.telemetry.enabled` or cleared using **Reset Run History**.
- **Automated Recipe Detection:** Smart detection of project files to generate auto-detected shortcuts. Organized into logical subfolders (**GitHub**, **Build & Run**, **Workspace**, **Scheduled**, and **Saropa Suite**) with distinct color-coded icons. Actions supported include **url** (open link), **shell** (run command line), **command** (invoke VS Code command), and **macro** (ordered sequence). Detection is cached per folder for maximized load performance.
- **Scheduled Rituals:** Time-triggered recipes that run unattended and capture output to a dated file under `reports/`. Designed for tasks like dawn lint sweeps (featuring first-class Dart/Flutter support via `saropa_lints`), sunrise stats, standup digests, and security audits. Seeded as disabled by default until explicitly promoted to a shortcut.
- **Saropa Suite Integration:** Automated detection of sibling Saropa tools to populate dedicated subfolders in the Recipes section:
  - *Saropa Lints:* Run analysis, open Code Health dashboard, manage rule packs, open Package Vibrancy, and export OWASP reports.
  - *Saropa Drift Advisor:* Open browser viewer, SQL Notebook, offline Dart schema scans, schema diagrams, and portable reports.
  - *Saropa Log Capture:* Open capture logs, search logs, export session Flow Maps, and show the Signals panel.
- **Project Files View:** A read-only sidebar view listing key project files (README, CHANGELOG, manifests like `package.json`, licenses). Displays relative last-modified times and declared versions parsed from manifests or changelogs. Sorted alphabetically by filename.
- **Copy Path Action:** Right-click context action to copy a file's absolute path to the clipboard, supported across shortcuts, recipes, and Project Files lists. Copies action targets for non-file recipes.
- **Storage and Data Model:** Project shortcuts persist to `.vscode/saropa-workspace.json` (auto-created empty if missing), while global shortcuts save to the extension's synced `globalState`. Includes a synthesized **Workspace config** example shortcut in empty environments.
- **Configurable Auto-Shortcuts:** Glob pattern-based auto-shortcut seeding with sticky removal state and on-demand restoration.
- **Execution Environments:** Support for script execution via the integrated terminal or a background output channel, featuring per-shortcut prefixes, arguments, environment variables, and custom working directories.
- **Favorites Import:** One-time per-workspace prompt to import existing shortcuts from `.favorites.json` (kdcro101 format) or scan sibling directories using the **Scan Sibling Projects for Favorites...** command.
- **Run-Parameters & Schedule Editors:** Interactive QuickPick and input box management flows ("Configure Run...", "Configure Schedule...") to adjust shortcut parameters, terminal modes, and recurrence timers without hand-editing JSON.
- **Background Execution Management:** Live tracking of background and scheduled tasks with a spinning status indicator and immediate process tree termination capabilities (including Windows child processes).
- **Placeholder & Interactive Tokens:** Support for runtime-expanded tokens (`$workspaceRoot`, `$dir`, `$file`) alongside interactive tokens like `${prompt:Label}` and `${pick:a,b,c}` for dynamic execution parameters.
- **Shortcut Groups with Drag-and-Drop:** Ability to create named folders within Project and Global scopes, supporting arbitrary reordering and drag-and-drop mechanics.
- **Run-Target Inference:** Auto-detects targets when adding files like `package.json` (npm, pnpm, yarn, bun), `Makefile` (`make <target>`), or shebang scripts as shortcuts to instantly seed exact execution configurations.
- **Global Run Palette:** Global command `Saropa Workspace: Run Shortcut...` provides a searchable quick pick of all workspace and global shortcuts, indexed by recency.
- **Smart Shortcut Suggestions:** Proactively offers to add frequently opened files as shortcuts once they clear a configurable threshold (default: 6 opens), manageable via `saropaWorkspace.suggestions.enabled`.
- **Status Bar Integration:** A specialized status-bar item showcasing upcoming scheduled executions. Clicking the item highlights and expands the respective shortcut in the tree.
- **Custom Presentation Overrides:** "Set Icon & Color..." context options to assign curated product icons and theme-aware colors to distinct shortcuts for easy visual categorization.
- **Run Shortcut with Overrides:** Execute shortcuts with one-off argument, directory, or environment variable modifications without altering the underlying saved configurations.
- **High-Performance Architecture:** Instant shortcut operations powered by cached folder-level glob scanning to avoid full workspace rescans during modifications.
- **Robust Submenu Targeting:** Workspace Shortcut submenu handles row-level Explorer and Project File interactions without triggering duplication warnings.
- **Context-Aware Tab Pinning:** Tab context menu bindings accurately track right-clicked file URIs rather than falling back to the active editor.
- **Accidental Execution Safeguards:** Single-clicking heavy or shell-based recipes opens a descriptive modal workflow rather than triggering side-effects immediately.
- **Process Termination Visibility:** Stopping an active background run updates the UI state with a distinct **stopping...** badge until process exit validation.
- **Fail-Safe Terminations:** Graceful background stops automatically escalate to a forced kill sequence if unresponsive, alongside an explicit **Force Kill** user action.
- **Asynchronous Multi-Root Loading:** Out-of-band, parallel filesystem probing ensures workspace rendering is never blocked by recipe discovery routines across multi-root structures.
- **Header Context Isolation:** Tree-view structural group headers (Project Shortcuts / Global Shortcuts) explicitly ignore individual shortcut actions like Rename or Remove.
- **Smart Double-Click Router:** Double-clicking non-runnable assets (e.g., Markdown, images) safely opens them within the native editor workspace with an explanatory notice rather than piping raw paths to a shell instance.
